export class Camera {
    constructor(cnv, ctx, x, y, persos) {
        this.cnv = cnv;
        this.ctx = ctx;
        this.position = { x, y};
        this.tabPersos = persos;
    }

    update(time){

    }

    draw(){
        
    }
}